{
  "name": "roe-back",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "ts-node-dev --transpile-only --no-notify --exit-child src/index.ts",
    "buildSchema": "npx ts-node --transpile-only src/schema"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "apollo-server": "^3.10.1",
    "graphql": "^16.5.0",
    "nexus": "^1.3.0"
  }
}
